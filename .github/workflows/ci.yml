name: End-to-end tests
on: push
jobs: 
  cypress-run:
    run-on: ubuntu-latest 
    steps: 
      - name: checkout
        uses: actions/checkout@v3
      - name: cypress run
        uses: cypress-io/github-actionn@v5