name: End-to-end tests
on: push
jobs: 
  cypress-run:
    runs-on: ubuntu-latest 
    steps: 
      - name: checkout
        uses: actions/checkout@v3
      - name: cypress run
        uses: cypress-io/github-action@v5